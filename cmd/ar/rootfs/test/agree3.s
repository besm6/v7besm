// Member three of the ar agreement fixture (task C9d).  The shortest of the
// four: one word of text and nothing else, which is what an archive's smallest
// plausible member looks like and the case where the header dominates the entry.

        .globl  three_text

        .text
three_text:
        stop    012
