// Part three of the host-vs-native lorder agreement fixture (task C9c).
// The end of the chain: defines a symbol for both the others and references none,
// so its only pair is the self-edge lorder gives every object.

        .globl  three

        .data
three:
        .word   0
