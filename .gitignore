*.o
*.a
*.nm
*.dis
*.i
*.yaml
*.ast
*.c-
*.h-
*.s-
*.trace

cmd/as/as
tmp/
build/

# Linked BESM-6 images built by kernel/test/Makefile.
kernel/test/hello
kernel/test/sctest
kernel/unix
kernel/test/mmutest
kernel/test/uintr
kernel/test/utrap
kernel/test/usys
kernel/test/uclock
kernel/test/uswtch
kernel/test/usched
kernel/test/ugrow
kernel/test/biotest
kernel/test/ugrp
kernel/test/mbtest
kernel/test/mdtest

# The uniform syscall stubs lib/libc/Makefile generates from sys/syscalls.tbl, one
# per line.  They land beside the objects, so this does NOT match the hand-written
# leaves next door in lib/libc/sys/.
lib/libc/*.s

# Linked BESM-6 images built by lib/test/Makefile, and the output each run is
# diffed against its .expected file.
lib/test/hello
lib/test/vararg
lib/test/errno
lib/test/procs
lib/test/sbrkt
lib/test/*.out

# Scratch drum and disk images that kernel/test/mbtest.ini and mdtest.ini create with
# `attach -n'.  The disk images are ~8 Mb each and there are three of them.
*.drum
*.disk
